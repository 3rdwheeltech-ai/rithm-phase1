# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.env
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.pyc

# JS/Node
node_modules/
web/dist/

# Secrets / local config
.env.local
*.pem
*.key
# Account-scoped infrastructure: ALB DNS, RDS endpoint, queue/topic ARNs,
# cluster and log-group names. Not secret in the credential sense, but not for
# a public repo either — and `git add -A` would otherwise sweep both in.
.env.arn
taskdef.json
session-manager-plugin.deb

# IDE
.vscode/
.idea/
*.DS_Store

# Test artifacts
playwright-report/
test-results/

# Internal docs — not for the public repo
docs/FrontendReferences/
docs/local-misc/
docs/others/terminal-progress-misc.md
docs/InitialSetup/RITHM_Phase1_DesignDoc-2.md
.env.rds
.env.rds.admin
